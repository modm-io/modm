#!/usr/bin/env python
#
# $Id$

import os

Import('env')
Import('sourceFiles')

print "tests"

# build a object file form each source file
for file in sourceFiles:
    print file.name
    basename = os.path.basename(file.name)
    name = os.path.splitext(basename)
    print name[0]
    env.Program(name[0], file.name)
    
