// coding: utf-8
/* Copyright (c) 2013, Roboterclub Aachen e.V.
 * All Rights Reserved.
 *
 * The file is part of the xpcc library and is released under the 3-clause BSD
 * license. See the file `LICENSE` for the full license governing this code.
 */
// ----------------------------------------------------------------------------

#ifndef XPCC__PLATFORM_HPP
#define XPCC__PLATFORM_HPP

/**
 * \ingroup 	architecture
 * \defgroup	platform	Supported Platforms
 *
 */

#include <stdint.h>

#include "detect.hpp"		// sets XPCC__CPU_xx etc. macros
#include "utils.hpp"

// hack to enable compilation of hosted targets at the same time as embedded targets
// this should go away when the library generator is finished
#if defined XPCC__CPU_HOSTED
#	if defined XPCC__OS_OSX
#		include "generated_platform_darwin/drivers.hpp"
#	else
#		include "generated_platform_linux/drivers.hpp"
#	endif
#else
// Include generated Platform sources:
#	include "${include_path}"
#endif

#endif	// XPCC__PLATFORM_HPP
