
def init(module):
    module.name = "utils"

def prepare(module, options):
    return True

def build(env):
    env.outbasepath = "src/modm/utils"

    env.copy("../utils.hpp")
    env.copy(".", ignore=env.ignore_files("*.lb"))
