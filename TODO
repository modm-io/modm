Things needed to be done
========================

* Replace the architecture 'pc' with 'hosted' in SCons
* Remove xpcc/debug/logger/imp/std.hpp after the 16.12.2011
* Remove xpcc::FlexClock (defined in architecture/driver/clock.hpp)
* Implement Timer 9-14 for STM32
* Rework the description of the Logger!
* Check GraphicDisplay for remains of a uint8_t coordinates (now int16_t)
* Rework the STM32 CAN Filter Interface (needs better names)
* Implement CAN2 (make CAN1 a template)
* Check UART interface (char vs. uint8_t)