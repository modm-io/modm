[communication]
container = senderreceiver
source = ../../../communication/xml/communication.xml

[environment]
LINKCOM* = -lpthread -lzmqpp -lzmq -lsodium

[build]
device = hosted
buildpath = ${xpccpath}/build/linux/${name}
