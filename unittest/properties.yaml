# Default properties
#
# These values are fallback options which are only used when they are not
# overwritten by local properties.

project:
  name: unittest
  target:
    architecture: pc
    #device: 
    #clock:  

pc:
  template: main_pc.cpp.in
atxmega:
  template: main_xmega.cpp.in
atmega:
  template: main_mega.cpp.in
