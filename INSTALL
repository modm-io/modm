INSTALL - xpcc
==============

You need to build the xpcc:

 * gcc (for the target platform (x86, avr, ...)
 * scons (a Python build tool)
 
To build you have to run scons in the trunk-Folder. The resulting files will
be placed in the trunk/build folder

